# ============================================================================
# 听籁 SoundVerse · 前端环境变量示例（复制为 .env / .env.local 后按需修改）
# ----------------------------------------------------------------------------
# 这些都只是「前端构建期」变量，改完必须重新 `npm run build` 才会生效。
# 识别服务本身是 server/ 里的 Python 容器，与这里的变量无关。
# ============================================================================

# —— 识别服务地址（三选一，优先级从高到低）——
# 1) VITE_BIRDNET_ENDPOINT：完整接口地址，原样使用，最高优先级。
#    仅在你的反代路径不是 /api/recognize 时用，例如：
# VITE_BIRDNET_ENDPOINT=https://tinglai.dushiofcourses.cn/recognize

# 2) VITE_RECOGNIZE_API：只填「服务基址」（到域名/端口为止，不要带 /api/recognize）。
#    开发联调本机容器：
# VITE_RECOGNIZE_API=http://localhost:8000
#    生产同源反代（nginx 已把 /api/ 反代到本机 8000）时，留空即可：
# VITE_RECOGNIZE_API=

# 3) VITE_API_BASE_URL：对齐部署文档写法（可带 /api 后缀，前端会自动归一化）。
#    优先级最低，通常不用，除非你在部署文档里约定了它的名字。
# VITE_API_BASE_URL=https://tinglai.dushiofcourses.cn/api

# 识别请求超时（毫秒），默认 30000
# VITE_RECOGNIZE_TIMEOUT=30000

# ============================================================================
# 速查：
#   · 开发本机起 server 容器  →  设 VITE_RECOGNIZE_API=http://localhost:8000
#   · 生产同源反代（推荐）    →  VITE_RECOGNIZE_API 留空（走相对路径 /api/recognize）
#   · 识别服务在另一台机器    →  VITE_RECOGNIZE_API=https://api.你的域名.com
# ============================================================================
