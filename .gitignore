node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
.env
.env.*
!.env.example

# 本地临时校验/日志产物（构建与类型检查/验证时生成，不入库）
# 统一忽略所有下划线前缀的临时文件（_*.txt / _*.cjs / _*.html / _*.js / _*.json 等），
# 避免 `git add .` 一把梭把验证脚本与中间产物带进仓库。
*.log
_*
commit_message.txt
probe.txt
# Vite 以 SSR 模式加载配置时产生的临时文件
vite.config.ts.timestamp-*

# 管理员工具（/dev）的本地产物：
#   每次保存自动生成的数据备份，属本机快照，不入库
*.bak.*
#   音频原始外链存档，供「按源重抓」用，机器相关
.audio-origins.json

# 识别接口压测产物（scripts/probe_recognition.py 的输出与音频劣化样本），机器相关不入库
.rectest/

# 本地打包上传服务器用的压缩包（dist.zip / server.zip 等）
*.zip
